import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ------------------------------------------------------------
  // BANO QABIL BRAND COLORS
  // ------------------------------------------------------------

  static const banoQabilGreen = Color(0xFF006B3C);
  static const banoQabilTeal = Color(0xFF009B87);
  static const banoQabilLightGreen = Color(0xFF8BC53F);

  // ------------------------------------------------------------
  // PRIMARY APP COLORS
  // ------------------------------------------------------------

  static const primary = banoQabilGreen;
  static const primaryDark = Color(0xFF004D2C);
  static const primaryLight = Color(0xFF16805A);

  static const accent = banoQabilTeal;
  static const accentLight = Color(0xFFE2F5F2);

  // ------------------------------------------------------------
  // BACKGROUND
  // ------------------------------------------------------------

  static const background = Color(0xFFF6F8FB);
  static const surface = Colors.white;

  // ------------------------------------------------------------
  // TEXT
  // ------------------------------------------------------------

  static const textPrimary = Color(0xFF17202A);
  static const textSecondary = Color(0xFF68737D);
  static const textLight = Color(0xFF9AA4AD);

  // ------------------------------------------------------------
  // STATUS
  // ------------------------------------------------------------

  static const success = Color(0xFF2E9B61);
  static const warning = Color(0xFFE59F22);
  static const error = Color(0xFFD9534F);
  static const info = Color(0xFF4285F4);

  // ------------------------------------------------------------
  // UI
  // ------------------------------------------------------------

  static const border = Color(0xFFE3E8ED);
  static const divider = Color(0xFFE9EDF1);
  static const shadow = Color(0x14000000);

  static const errorBackground = Color(0xFFFDECEC);
  static const surfaceVariant = Color(0xFFF1F4F6);
}